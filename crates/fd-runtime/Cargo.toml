[package]
name = "fd-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FlightdeckOS runtime: session sequencing, snapshot ingestion, phase tracking, action execution pipeline, append-only trace"

[dependencies]
fd-core = { path = "../fd-core" }
fd-sop = { path = "../fd-sop" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = "3.17"
fd-aircraft = { path = "../fd-aircraft" }
