[package]
name = "fd-xplm-operator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Operator flight-preparation helper plugin (one-shot operator FMS plan load); NOT part of the read-only observation path."

[lib]
crate-type = ["cdylib"]

[package.metadata.notes]


[dependencies]
