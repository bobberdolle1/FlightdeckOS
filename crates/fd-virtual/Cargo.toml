[package]
name = "fd-virtual"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FlightdeckOS headless virtual simulator: semantic systems model + bounded kinematic flight model + deterministic clock (TEST MODEL, not real aircraft performance)"

[dependencies]
fd-core = { path = "../fd-core" }
serde.workspace = true
